<!-- workrally-mcp-app -->
本仓库是 WorkRally MCP App。若还没有应用源码，按 `.agents/skills/workrally-mcp-app/SKILL.md` 的「从零搭建」用 vendor 里的 `templates/mcp-app` / `scripts/scaffold.mjs` 生成骨架。改 `ui/` / `mcp-server/` / `.mcp.json` 前必读该 SKILL，并按其中 `vendor/` 查阅规范。该 skill 由 WorkRally 打开应用目录时覆盖写入，勿手改。
<!-- /workrally-mcp-app -->
