/**
 * 内置进 Codex 的 SKILL.md 模板。
 *
 * 为什么要写在应用里而不是直接放文件：
 * 命令行入口的绝对路径随安装位置变化，模板要按当前 appRoot 生成，
 * 否则内置出去的命令会指向一个不存在的路径。
 */

/** 生成 SKILL.md 正文。appRoot 是应用根目录的绝对路径。 */
export function CODEX_SKILL_MD(appRoot: string): string {
  // Codex 里的路径用正斜杠更稳（Windows 反斜杠在 markdown 代码块里容易被转义）
  const cli = `${appRoot.replace(/\\/g, '/')}/mcp-server/cli.ts`;
  const pkg = `${appRoot.replace(/\\/g, '/')}/node_modules/tsx/dist/cli.mjs`;

  return `---
name: skill-foundry
description: Skill 出厂台 —— 体检技能库的「触发路由」与「关系结构」。当用户问「我的技能为什么没被调用 / 装了不生效 / 技能是不是重复了 / 帮我看看技能库有什么问题 / 触发词冲突」，或要给技能库做体检、找重复技能、看技能依赖关系时使用。也能回答"这句话会触发哪个技能"。
---

# Skill 出厂台

体检一个技能库的两件事：**装了会不会被叫到**（触发路由）、**它们之间什么关系**（引用/重复/孤岛）。

与静态质量审计互补：本工具管**运行时命中**。

## 什么时候用

- 用户提到「技能没被触发 / 装了没用 / 叫不出来」
- 用户怀疑技能重复、不知道该删哪个
- 用户想给技能库做一次全面体检
- 用户问「这么说会触发哪个技能」

## 怎么跑

命令行的入口（用 \`tsx\` 直接跑 TypeScript）：

\`\`\`bash
# 进到应用目录
cd ${appRoot.replace(/\\/g, '/')}

# 静态体检（不花模型，最快，先跑这个）
node ${pkg} ${cli} static

# 试一条：看一句话会叫出哪些技能
node ${pkg} ${cli} probe "帮我做个 30 秒的打斗视频"

# 关系图谱：域间依赖 / 孤岛 / 疑似重复
node ${pkg} ${cli} graph

# 批量路由体检（需要语料 + 模型）
node ${pkg} ${cli} corpus --mode observe   # 先生成观察语料
node ${pkg} ${cli} route --runs 3          # 再跑体检

# 看 / 测模型接入
node ${pkg} ${cli} providers
node ${pkg} ${cli} test
\`\`\`

加 \`--json\` 输出原始 JSON（便于程序消费）；加 \`--root <dir>\` 只扫描指定技能库。

## 怎么读结果

**静态体检**（不需要模型）：
- **线索词冲突**：同一个词被多个技能声明 → 模型分不清该用谁。标了「职责相近」的优先修。
- **描述高度重合**：两个技能用词几乎一样 → 考虑合并。
- **没测过的技能**：没有语料覆盖，装了会不会被叫到没人知道。

**试一条 / 路由体检**：
- \`●\` 稳定命中（每次都对）
- \`◐\` 摇摆触发（时灵时不灵 —— 描述边界模糊）
- **漏触**：该触发却没触发（线索词没覆盖用户的说法）
- **误触**：不该触发却触发（描述写太宽）

## 怎么改（重要）

本工具**只读技能库，绝不修改任何 skill 文件**。发现的问题要**用户确认后**再动手改，
改的部位通常是 \`description\` 里的触发词与适用/不适用边界：

- **漏触** → 补上用户真实会说的说法（口语、同义词、缩写）
- **误触** → 加一句「不要用于 …」
- **摇摆** → 把「什么情况下一定用我 / 什么情况下绝不用我」都写清楚
- **抢触发** → 二选一：线索词各收窄一半，或合并成一个技能

## 数据在哪

所有输出（语料、报告、译名、模型配置）都在 \`~/.skill-foundry/\`，
和图形界面共用同一份，**不写进技能库**。

## 不适用

- 只想知道某个技能写了什么 → 直接读那个 SKILL.md
- 技能库的静态语法错误（frontmatter 格式等）→ 那是静态审计工具的事
`;
}
